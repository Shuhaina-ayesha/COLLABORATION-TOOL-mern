* { margin: 0; padding: 0; box-sizing: border-box; }
body { background: #f0f2f5; font-family: 'Segoe UI', sans-serif; overflow: hidden; }

.app { height: 100vh; display: flex; flex-direction: column; }
header {
  background: #3182ce;
  color: white;
  padding: 1rem;
  display: flex;
  align-items: center;
  gap: 1rem;
  flex-wrap: wrap;
}
header h1 { margin: 0; flex: 1; }
.tools { display: flex; gap: 1rem; align-items: center; }
.tools label { display: flex; align-items: center; gap: 0.5rem; font-size: 0.9rem; }
.tools input[type="range"] { width: 100px; }

canvas {
  flex: 1;
  background: white;
  cursor: crosshair;
  touch-action: none;
}

@media (max-width: 768px) {
  header { flex-direction: column; align-items: stretch; }
  .tools { justify-content: center; }
  canvas { max-height: 70vh; }
}
